% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regex.R
\name{build_exact_pattern}
\alias{build_exact_pattern}
\title{Build an exact-match regex}
\usage{
build_exact_pattern(x, collapse = "|", fixed = TRUE)
}
\arguments{
\item{x}{Character vector of patterns.}

\item{collapse}{String used to join multiple patterns (default: \code{"|"}).}

\item{fixed}{Logical; if \code{TRUE} (default), escape regex metacharacters in \code{x}
so they are treated literally. If \code{FALSE}, \code{x} is treated as raw regex.}
}
\value{
A length-1 character string containing the regex. Use with
\code{grepl(..., perl = TRUE)}.
}
\description{
Construct a regex that matches input strings \strong{exactly} (i.e., with
start/end anchors). When multiple inputs are provided, they are joined
with OR (\code{|}) and wrapped with \verb{^(?: ... )$}.
}
\examples{
\donttest{
x <- c("apple", "banana")
pattern <- build_exact_pattern(x)
grepl(pattern, c("apple", "banana", "pineapple"), perl = TRUE)
# [1] TRUE TRUE FALSE
}

}
