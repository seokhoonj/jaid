% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmd.R
\name{mkdir}
\alias{mkdir}
\title{Create directories}
\usage{
mkdir(
  folder = c("dev", "data", "info", "inst", "output", "R", "raw", "report", "rules")
)
}
\arguments{
\item{folder}{A character vector of directory paths to create.}
}
\value{
No return value, called for side effects. Creates the specified
directories. If a directory already exists, a message is shown and
no action is taken.
}
\description{
Create directories on disk, similar to the Linux \code{mkdir} command.
Directories are created recursively if needed.
}
\examples{
\dontrun{
# Create two temporary directories
mkdir(c("tmp1", "tmp2"))

# Re-running will show "Already exists"
mkdir(c("tmp1", "tmp2"))
}

}
