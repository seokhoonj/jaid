% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regex.R
\name{grepl_all}
\alias{grepl_all}
\title{Match only if all patterns are present (AND)}
\usage{
grepl_all(pattern, x, ignore.case = TRUE)
}
\arguments{
\item{pattern}{Character vector of regular expressions that must all match.}

\item{x}{Character vector to search.}

\item{ignore.case}{Logical; if \code{TRUE}, case is ignored during matching.}
}
\value{
Logical vector the same length as \code{x}.
}
\description{
Vectorized helper around \code{\link[base:grep]{base::grepl()}} that returns \code{TRUE} only when
every pattern in \code{pattern} is found in \code{x}. Internally, it builds a single
regular expression using positive lookahead and evaluates it with
\code{perl = TRUE}.
}
\examples{
x <- c("abcde12345", "xyz", "a1c")
grepl_all(c("a", "c", "[1-5]"), x)
grepl_all(c("foo", "bar"), c("foo bar", "foo", "bar"))

}
\seealso{
\code{\link[base:grep]{base::grepl()}}
}
