% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{col_sum}
\alias{col_sum}
\alias{col_max}
\alias{col_min}
\title{Column-wise sum, maximum, and minimum}
\usage{
col_sum(x, na.rm = TRUE)

col_max(x, na.rm = TRUE)

col_min(x, na.rm = TRUE)
}
\arguments{
\item{x}{A numeric matrix.}

\item{na.rm}{Logical. Should missing values (\code{NA}) be removed?
If \code{TRUE} (default), they are ignored; if \code{FALSE}, any \code{NA} in a column
makes the result \code{NA}.}
}
\value{
A numeric vector of length equal to the number of columns in \code{x}:
\itemize{
\item \code{col_sum()} returns the sum of each column.
\item \code{col_max()} returns the maximum of each column.
\item \code{col_min()} returns the minimum of each column.
}
}
\description{
Compute the sum, maximum, or minimum of values in each column of a numeric
matrix. These functions are implemented in C for performance.
}
\examples{
\donttest{
x <- matrix(c(1, 2, NA, 4, 5, 6), ncol = 3)

# Column-wise sum
col_sum(x)

# Column-wise maximum
col_max(x)

# Column-wise minimum (preserve NA)
col_min(x, na.rm = FALSE)
}

}
