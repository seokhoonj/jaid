% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{write_data_xlsx}
\alias{write_data_xlsx}
\title{Write data to an Excel workbook}
\usage{
write_data_xlsx(
  data,
  file,
  rc = c(1L, 1L),
  row_names = FALSE,
  overwrite = FALSE,
  font_size = 14,
  font_name = "Comic Sans MS",
  border_colour = "#000000",
  widths = 8.43
)
}
\arguments{
\item{data}{A data.frame, or a named/unnamed list of data.frames. When a list
is provided, each element is written to its own worksheet.}

\item{file}{A file path to the target \code{.xlsx} workbook.}

\item{rc}{A length-2 integer vector giving the starting row and column
(default \code{c(1L, 1L)}).}

\item{row_names}{Logical; whether to include row names.}

\item{overwrite}{Logical; if \code{TRUE}, overwrite an existing workbook.}

\item{font_size}{Numeric font size for the Excel output (default \code{14}).}

\item{font_name}{Font family name for the Excel output (default \code{"Comic Sans MS"}).}

\item{border_colour}{Border color used by the writing helper (HEX string, e.g., \code{"#000000"}).}

\item{widths}{Column widths passed to the writing helper. Either a single
numeric value recycled across columns or a numeric vector per column.}
}
\value{
No return value, called for side effects (saves an Excel file).
}
\description{
Write a data frame or a list of data frames to an Excel workbook using
\code{openxlsx}. When a list is supplied, each element is written to a
separate worksheet. List names are used as sheet names; unnamed elements
receive autogenerated names (e.g., "Sheet 1").
}
\examples{
\dontrun{
write_data_xlsx(
  list(cars = cars, mtcars = mtcars),
  file = "data.xlsx",
  rc = c(1L, 1L),
  overwrite = TRUE
)
}

}
