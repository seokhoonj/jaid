% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{mat-ops-inplace}
\alias{mat-ops-inplace}
\alias{matXmat}
\alias{matXrow}
\alias{matXcol}
\alias{matXnum}
\title{In-place, memory-efficient matrix multiplications}
\usage{
matXmat(x, y)

matXrow(mat, vec)

matXcol(mat, vec)

matXnum(mat, num)
}
\arguments{
\item{x, y, mat}{A numeric matrix (modified in place).}

\item{vec}{A numeric vector (row or column operand).}

\item{num}{A numeric scalar.}
}
\value{
No return value, called for side effects.
}
\description{
Perform matrix multiplications \strong{in place}:
the result is written back into the memory of the first matrix argument,
avoiding additional allocations. Useful for large matrices and
tight memory budgets.
}
\section{What “in place” means}{

\itemize{
\item The first matrix argument is \strong{modified} directly.
\item No new object is returned (called for side effects).
\item Copy the matrix first if you need to preserve it.
}
}

\examples{
\dontrun{
x <- matrix(1:4, 2, 2)
y <- matrix(5:8, 2, 2)

matXmat(x, y)       # matrix × matrix
matXrow(x, c(1, 2)) # matrix × row vector
matXcol(x, c(3, 4)) # matrix × column vector
matXnum(x, 10)      # matrix × scalar
}
}
