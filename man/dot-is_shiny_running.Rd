% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{.is_shiny_running}
\alias{.is_shiny_running}
\title{Check if a Shiny session is running}
\usage{
.is_shiny_running()
}
\value{
A logical scalar: \code{TRUE} if \code{shiny} is running, \code{FALSE} otherwise.
}
\description{
This utility inspects whether Shiny is currently running.
}
\details{
\itemize{
\item If the \code{shiny} namespace is already loaded, it looks up and safely
calls \code{shiny::isRunning()}.
\item If \code{shiny} is not loaded, it simply returns \code{FALSE}.
\item Any errors from \code{shiny::isRunning()} are caught and treated as \code{FALSE}.
}
}
\examples{
\dontrun{
# Returns FALSE in non-Shiny environments
# In a Shiny app, will return TRUE once the session is active
.is_shiny_running()
}

}
\keyword{internal}
