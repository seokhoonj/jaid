% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{row_sum}
\alias{row_sum}
\alias{row_max}
\alias{row_min}
\title{Row-wise sum, maximum, and minimum}
\usage{
row_sum(x, na.rm = TRUE)

row_max(x, na.rm = TRUE)

row_min(x, na.rm = TRUE)
}
\arguments{
\item{x}{A numeric matrix.}

\item{na.rm}{Logical. Should missing values (\code{NA}) be removed?
If \code{TRUE} (default), they are ignored; if \code{FALSE}, any \code{NA} in a row
makes the result \code{NA}.}
}
\value{
A numeric vector of length equal to the number of rows in \code{x}:
\itemize{
\item \code{row_sum()} returns the sum of each row.
\item \code{row_max()} returns the maximum of each row.
\item \code{row_min()} returns the minimum of each row.
}
}
\description{
Compute the sum, maximum, or minimum of values in each row of a numeric
matrix. These functions are implemented in C for performance.
}
\examples{
\donttest{
x <- matrix(c(1, 2, NA, 4, 5, 6), nrow = 2)

# Row-wise sum
row_sum(x)

# Row-wise maximum
row_max(x)

# Row-wise minimum (preserve NA)
row_min(x, na.rm = FALSE)
}

}
