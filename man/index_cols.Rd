% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{index_cols}
\alias{index_cols}
\title{Index columns by name}
\usage{
index_cols(x, cols, all_matches = FALSE, error_on_missing = TRUE)
}
\arguments{
\item{x}{A data.frame or matrix.}

\item{cols}{A character vector of column names (in desired order).}

\item{all_matches}{Logical; if \code{TRUE}, return indices of \emph{all} columns whose
names match each \code{cols} entry (duplicates allowed). If \code{FALSE} (default),
return only the first match for each name.}

\item{error_on_missing}{Logical; if \code{TRUE} (default), error when any of
\code{cols} are not present in \code{colnames(x)}. If \code{FALSE}, drop missing names.}
}
\value{
Integer vector of column indices. Names of the vector correspond to the
requested \code{cols} that were successfully matched.
}
\description{
Return column indices for the given column names from a data.frame or matrix.
}
\examples{
# data.frame
index_cols(mtcars, c("disp", "drat", "qsec"))

# drop missing columns silently
index_cols(mtcars, c("disp", "drat", "nope"), error_on_missing = FALSE)

# matrix
mat <- as.matrix(mtcars)
index_cols(mat, c("disp", "hp"))

# duplicated column names (all matches)
df <- data.frame(a = 1, a = 2, b = 3, check.names = FALSE)
index_cols(df, "a", all_matches = TRUE)

}
