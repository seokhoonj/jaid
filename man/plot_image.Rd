% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image.R
\name{plot_image}
\alias{plot_image}
\title{Plot an Image file}
\usage{
plot_image(file, width = NULL, height = NULL, clear = TRUE)
}
\arguments{
\item{file}{Path or URL to a PNG image file.}

\item{width}{Optional width for the image, passed to \code{\link[grid:grid.raster]{grid::grid.raster()}}.
Can be a \code{\link[grid:unit]{grid::unit()}} object or \code{NULL} for default sizing.}

\item{height}{Optional height for the image, passed to \code{\link[grid:grid.raster]{grid::grid.raster()}}.
Can be a \code{\link[grid:unit]{grid::unit()}} object or \code{NULL} for default sizing.}

\item{clear}{Logical flag; if \code{TRUE} (default), a new page is started with
\code{\link[grid:grid.newpage]{grid::grid.newpage()}} before drawing the image.}
}
\value{
Invisibly returns the \code{magick-image} object read from \code{file}.
}
\description{
Reads an image using the \code{magick} package and displays it on the
current graphics device via \code{grid}.
}
\examples{
\dontrun{
plot_image(system.file("img", "Rlogo.png", package = "png"),
           width = grid::unit(0.1, "npc"),
           height = grid::unit(0.1, "npc"))
}

}
