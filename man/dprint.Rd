% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{dprint}
\alias{dprint}
\title{Print a data frame in a fixed-width table format}
\usage{
dprint(df, width, align = c("right", "both", "left"), n = 300L)
}
\arguments{
\item{df}{A \code{data.frame} to print.}

\item{width}{Integer. Target width for each column (passed to
\code{.adjust_names_width()}).}

\item{align}{Character string specifying text alignment within columns:
\code{"right"}, \code{"left"}, or \code{"both"}. Default is \code{"right"}.}

\item{n}{Integer. Number of rows to print (default: 300).}
}
\value{
No return value, called for side effects (prints to console).
}
\description{
Formats and prints a data frame with aligned column widths, similar to a
simple console table. Useful for quickly inspecting data frames with
controlled formatting.
}
\details{
\itemize{
\item The function adjusts column names and values to the specified width,
aligns text as requested, and prints the first \code{n} rows.
\item Column names are printed as a header row between horizontal rules
(via \code{cli::cat_rule()}).
\item Output is printed directly to the console using \code{cat()}.
}
}
\examples{
\donttest{
dprint(mtcars, width = 10, align = "left", n = 5)
}

}
